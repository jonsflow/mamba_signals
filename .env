# System type configuration
# Set SYSTEM_TYPE to determine which config to load
# - "dev": Uses config.py (Mac-optimized, small batch sizes)
# - "gpu": Uses gpu_config.py (NVIDIA GPU-optimized, large batch sizes)

SYSTEM_TYPE=dev
